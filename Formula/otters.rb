# typed: false
# frozen_string_literal: true

# This file was generated by GoReleaser. DO NOT EDIT.
class Otters < Formula
  desc "Build, run, and chat with AI agents"
  homepage "https://github.com/openotters/openotters"
  version "1.0.0-alpha.74"
  license "MIT"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/openotters/openotters/releases/download/v1.0.0-alpha.74/otters_darwin_amd64.tar.gz"
      sha256 "fa648c454eabafce04e6a0471597c43bf9f83cfc434307ae3103dafe4a2b7af4"

      define_method(:install) do
        bin.install "otters"
        bin.install "ottersd"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/openotters/openotters/releases/download/v1.0.0-alpha.74/otters_darwin_arm64.tar.gz"
      sha256 "0d391d7dee3cda76c4a7a00727b4b8e81205d3267c4d00feef0262da17b23bae"

      define_method(:install) do
        bin.install "otters"
        bin.install "ottersd"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/openotters/openotters/releases/download/v1.0.0-alpha.74/otters_linux_amd64.tar.gz"
      sha256 "26ab5b6ebbd723057307c256a581a07c93fa42bb7c683404d2baf5e689124485"
      define_method(:install) do
        bin.install "otters"
        bin.install "ottersd"
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/openotters/openotters/releases/download/v1.0.0-alpha.74/otters_linux_arm64.tar.gz"
      sha256 "89135bf3f38659eb88243512ea5d535368e730f154c644b8072aded2d59d6857"
      define_method(:install) do
        bin.install "otters"
        bin.install "ottersd"
      end
    end
  end

  def caveats
    <<~EOS
      The otters daemon (ottersd) has been installed but is not running.

      To run it in the background (launchd on macOS, systemd --user on Linux):
        brew services start otters

      To run it in the foreground for debugging:
        ottersd serve

      Default paths and endpoints:
        socket:        ~/.otters/otters.sock
        agent data:    ~/.otters/agents
        logs:          #{var}/log/ottersd.{log,err.log} (service mode only)
        web UI + API:  http://127.0.0.1:5500  (loopback by default)

      The daemon ships with an embedded web UI baked into the binary —
      no separate install. Once ottersd is running, open
      http://127.0.0.1:5500 in your browser. The Connect/gRPC API is
      reachable on the same listener under /api.

      Customise the listener:
        ottersd serve --http-addr 127.0.0.1:8080   # change the port
        ottersd serve --no-ui                      # API only
        ottersd serve --no-http                    # CLI only (Unix socket)

      Non-loopback binds require --auth-token.

      Get started once the daemon is running:
        otters --help
        otters run <Agentfile>
        open http://127.0.0.1:5500

      Stop the service:
        brew services stop otters
    EOS
  end

  service do
    run [opt_bin/"ottersd", "serve"]
    keep_alive successful_exit: false, crashed: true
    log_path var/"log/ottersd.log"
    error_log_path var/"log/ottersd.err.log"
  end

  test do
    assert_match "otters", shell_output("#{bin}/otters --version")
  end
end
